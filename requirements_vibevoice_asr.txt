# ============================================================
# VibeVoice ASR — Requirements
# Python 3.11 | CUDA 13.x (RTX PRO 6000 / ปรับตาม GPU เครื่องปลายทาง)
# ============================================================
# ขั้นตอนติดตั้ง:
#   1. conda create -n vibevoice_asr python=3.11
#   2. conda activate vibevoice_asr
#   3. pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu128
#      (ปรับ cu128 ตาม CUDA version ของเครื่องปลายทาง: cu118 / cu121 / cu124 / cu128)
#   4. pip install -r requirements_vibevoice_asr.txt
#   5. cd /path/to/VibeVoice && pip install -e .
# ============================================================

# --- Core ASR ---
transformers==4.57.6
accelerate==1.13.0
safetensors==0.7.0
tokenizers==0.22.2
huggingface_hub==0.36.2

# --- Audio Processing ---
librosa==0.11.0
soundfile==0.13.1
noisereduce==3.0.3
pydub==0.25.1
audioread==3.1.0
soxr==1.0.0

# --- Speaker Diarization (pyannote) ---
pyannote-audio==4.0.4
pyannote-core==6.0.1
pyannote-database==6.1.1
pyannote-metrics==4.0.0
pyannote-pipeline==4.0.0

# --- Scientific Computing ---
numpy==2.4.4
scipy==1.17.1
scikit-learn==1.8.0
numba==0.65.0
llvmlite==0.47.0

# --- Web UI (Gradio) ---
gradio==6.10.0
fastapi==0.135.3
uvicorn==0.42.0

# --- Utilities ---
tqdm==4.67.3
requests==2.33.1
python-dotenv==1.2.2
packaging==26.0
PyYAML==6.0.3
einops==0.8.2
ml_collections==1.1.0

# --- torch ไม่ใส่ที่นี่ ---
# ติดตั้งแยกก่อนด้วย --index-url ตาม CUDA version (ดูขั้นตอนข้างบน)
# torchaudio ต้องตรงกับ torch version เสมอ
